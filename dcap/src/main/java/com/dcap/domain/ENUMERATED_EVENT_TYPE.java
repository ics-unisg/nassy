package com.dcap.domain;

/**
 * The enum Enumerated event type.
 * if it has a duration, it is INSTANTANTANIOUS, else PHASE
 *
 * @author uli
 */
public enum ENUMERATED_EVENT_TYPE {
    INSTANTANIOUS,
    PHASE
}
